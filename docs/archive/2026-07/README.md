# ARCHIVED — 2026-07-30

Every file in this directory was written for a port that is no longer the
project. Retained for history. **Not authoritative. Do not cite as current
truth.** This applies to every file here including `inventory.json`, which
carries no inline banner so that it remains valid JSON.

Superseded by `docs/product/` (frozen documents) and
`docs/requirements/` (requirements evidence).

`backup-browser-data.js` is archived here too. It carries no inline banner,
for the same reason as `inventory.json` — it is executable JavaScript and a
banner would break it. It served the browser-data backup workflow abandoned
by owner decision on 2026-07-29.
